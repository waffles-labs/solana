# This package only exists to avoid circular dependencies during cargo publish:
# solana-runtime -> solana-address-program -> solana-program-test -> solana-runtime

[package]
name = "waffles-solana-address-lookup-table-program-tests"
version = "1.16.0-alpha.10"
authors = ["Solana Maintainers <maintainers@solana.foundation", "Waffle Labs Maintainers <maintainers@waffles.so>"]
repository = "https://github.com/waffles-labs/solana"
license = "Apache-2.0"
homepage = "https://waffles.so/"
edition = "2021"
publish = false

[dev-dependencies]
assert_matches = "1.5.0"
bincode = "1.3.3"
solana-address-lookup-table-program = { package = "waffles-solana-address-lookup-table-program", path = "../address-lookup-table", version = "=1.16.0-alpha.10" }
solana-program-test = { package = "waffles-solana-program-test", path = "../../program-test", version = "=1.16.0-alpha.10" }
solana-sdk = { package = "waffles-solana-sdk", path = "../../sdk", version = "=1.16.0-alpha.10" }

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]
